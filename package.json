{
  "name": "point-convex",
  "version": "3.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p convex/tsconfig.json",
    "package:extension": "mkdir -p dist && (cd point-extension && zip -r ../dist/point-extension.zip .)",
    "verify": "npm run typecheck && npm run test && npm run lint && npm run package:extension",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --max-warnings 0 --fix",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{js,cjs,mjs,ts}": "eslint --max-warnings 0 --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "convex": "^1.34.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@eslint/js": "^10.0.1",
    "convex-test": "^0.0.43",
    "eslint": "^10.1.0",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.57.2",
    "vitest": "^4.1.1"
  }
}
